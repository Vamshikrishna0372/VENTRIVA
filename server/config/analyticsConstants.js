/**
 * Centralized Analytics & Intelligence Constants
 */
const ANALYTICS_PERIODS = ['7d', '30d', '90d', '6m', '12m', 'all'];

const TREND_TYPES = ['positive', 'negative', 'neutral'];

const INSIGHT_PRIORITIES = ['low', 'medium', 'high', 'critical'];

const RECOMMENDATION_TYPES = [
  'startup',
  'pipeline',
  'evaluation',
  'profile',
  'engagement',
  'fundraising',
  'due_diligence',
];

module.exports = {
  ANALYTICS_PERIODS,
  TREND_TYPES,
  INSIGHT_PRIORITIES,
  RECOMMENDATION_TYPES,
};
